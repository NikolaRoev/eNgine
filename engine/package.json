{
  "type": "module",
  "scripts": {
    "update-dependencies": "ncu -u && cargo upgrade",
    "build-core": "wasm-pack build --target web --out-dir pkg/core",
    "build-runtime": "wasm-pack build --target web --out-dir pkg/runtime -- --features runtime"
  }
}
